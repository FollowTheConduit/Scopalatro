#version 460 core

in vec3 vNormals;
in vec2 vTexCoords;
in vec3 vColor;
in flat int vInstanceID;

out vec4 FragColor;

uniform sampler2DArray uAtlas;

void main() {

	vec4 tex = texture(uAtlas, vec3(vTexCoords, 0.0));
	FragColor = tex * vec4 (vColor, 1.0);
};